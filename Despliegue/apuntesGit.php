<?php

/*

* HEAD --> apunta a lo que estes enseñando en el directorio de trabajo
    ?  git checkout 
         -> puedes moverte por commits, el HEAD te enseñará el commit como si hubieras
            retrocedido en el tiempo, pero los commits siguen ahí, no se borran
    
    ?  git checkout -b nombreRama 
         -> creo y cambio de rama


* Ramas principales en función del entorno que estemos desarrollando
    - Master -> desarrollo
    - Main -> preproducción
    - ComoSeLlame -> producción
    - Ramificaciones -> feature _ pequeña descripción funcionalidad (aclaración de lo que estás haciendo)
                     -> hotfix _ pequeña descripción arreglar fallos

        > Una vez están ok, se borran las ramas


* Comandos nuevos:

    * git rebase -> reorganizar
    Haces un nuevo commit, excatamencte igual a otro commit, pero vas a hacer una reorganización, coge el ancestro
    común, reorganiza, y los cambios que tiene en un commit los suma a otro, aparece como una sola línea del tiempo


    * git reset -> 







*/

?>